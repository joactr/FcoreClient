import React, {Fragment, useState} from 'react';
import SideNav from "./sideNav/SideNav";

const Home = () => {
  return (
      <Fragment>
        <SideNav/>
      </Fragment>
  );
}

export default Home;
